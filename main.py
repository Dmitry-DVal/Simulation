# https://zhukovsd.github.io/python-backend-learning-course/Projects/Simulation/#herbivorefrom config import Configfrom Actions.map import Mapfrom Actions.simulation import Simulationm = Map(Config.height, Config.width) #(x-8, y-15)s = Simulation(m, Config())s.set_entities_to_map()m.show_map()# print('-'*10)# Проверка, что ЭК перемещается на указанное поле# s.next_turn((0, 1))# m.show_map()# print('-'*10)# m.show_map()# Проверка, что у ЭК есть нужные атрибуты# print(s.Map.map[0,0])# print(dir(s.Map.map[0,0]))# print(s.Map.map[0,0].image, 'hp = ', s.Map.map[0,0].hp)# m_test = Map(5, 8)# s_test = Simulation(m, Config())# s_test.set_entities_to_map()# m_test.show_map()# print(s_test.Map.map)